# AGENTS.md

本仓库是「灵感徽章 · RealityBadge」的官网（GitHub Pages 静态站）。

**任何 AI 代理在本仓库工作时，必须遵守下面三条。**

---

## 一 · 先读设计规范

动手前先读 [`docs/DESIGN.md`](docs/DESIGN.md)。它定义了四件必须先定好的事：
**叙事结构 / 视觉基调 / 动效语言 / 硬性约束**。

改视觉或动效之前先改那份文件，再改代码 —— 顺序反了就会出现"代码里有一套风格，
文档里写着另一套"的状态。

硬性约束的要点（完整版在 DESIGN.md 第四节）：
1. 不加载任何第三方脚本（three.js 走本地 vendor）
2. 不加载任何图片素材（视觉全部代码生成）
3. 无追踪、无统计、无外部请求
4. `prefers-reduced-motion` 必须降级
5. 移动端可用，触屏拖拽同等生效
6. 不抄参考站的视觉母题（尤其"地球"）

---

## 二 · 必须自动记录自己的行为

**每次会话结束前，在 [`docs/AGENT-LOG.md`](docs/AGENT-LOG.md) 追加一条记录**（最新在最上面）。
这不是可选项 —— 上下文会被压缩，后来接手的代理只能靠这份日志判断"什么已经做过、什么验证过"。

每条记录包含四项，缺一不可：

```markdown
## YYYY-MM-DD · 一句话说明这次做了什么

**改了什么**：文件级清单（哪些文件、加了什么）
**为什么**：决策理由。不要写"优化了性能"，要写"首屏 3.2s 是因为三个点阵各算了一次，
合并成一次采样后 0.9s"。理由比结论更值钱。
**验证了什么**：每条都要有**证据**（截图路径 / 命令 / 数字）。
**没验证什么 / 已知问题**：诚实列出来。写"已全部完成"但没证据，等于没做。
```

同时记录任何**踩到的坑**（命令、环境、浏览器行为），格式：
`坑：现象 → 根因 → 绕法`。

---

## 三 · 不许把没验证的东西说成完成了

- 改了视觉或动效 → 必须**无头截图**看一遍（命令见 DESIGN.md 第五节），并把截图路径写进日志。
- 改了点阵/几何数据 → 必须报出**数字**（点数、耗时），不许只说"更流畅了"。
- 拿不准的审美判断 → 交给**评委子代理**（全新上下文、只看截图、不给代码）打分，
  把分数和意见记进日志。
- 无法验证的部分，明确写"未验证"及原因。

---

## 附 · 本地预览

```bash
python3 -m http.server 8899 --directory .
open "http://127.0.0.1:8899/"
```

时间轴参数（供截图验证用）：`?p=0..1` 直接跳到整页进度的某个位置。
